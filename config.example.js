// config.example.js — TEMPLATE. Copy to config.js and fill in your credentials.
// config.js is gitignored and must never be committed.
window.APP_CONFIG = {
  username: "YOUR_USERNAME",
  password: "YOUR_PASSWORD"
};
